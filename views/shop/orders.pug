extends ../layouts/main-layout.pug

block content
    main
        if order
            ul
                h1 #{ order._id }
                ul
                    each product in order.items
                        li #{ product.product.title } ( #{ product.quantity } )

        else
            h1 You don't have any orders
